WOW LAYER (MANDATORY): Upgrade this design from “wireframe shell” to “Apple/Dyson-level craft”.
The IA and content sections remain unchanged, but every section must have:
- a hero visual moment
- one interactive or motion detail
- premium material depth (subtle glass, shadows, gradients)
- structured scientific visuals (not text blocks)

A) PREMIUM VISUAL CRAFT RULES (NON-NEGOTIABLE)
1) IMAGE-FIRST STORYTELLING
- Each major section must include at least one of:
  - full-bleed studio packshot
  - macro texture frame
  - in-use device shot
  - short loop video (6–12s) with muted playback
- Replace “empty text blocks” with image-led layouts and captions.
- Use Leica-like restraint: fewer but higher-impact images.

2) LIGHTING-LIKE GRADIENTS (NOT DECORATIVE)
- Use subtle studio-light gradients on white:
  - white → faint blue bloom → white (max 6–8% intensity)
- Use soft vignettes behind product renders to create depth.
- Never use neon or strong glow.

3) GLASS PANELS AS MATERIAL (SUBTLE)
- Use frosted glass overlays for tooltips, modals, and “science cards”:
  - 8–12% white opacity
  - blur 16–22
  - hairline border
  - soft shadow
- Glass should feel like Apple Control Center, not crypto UI.

4) APPLE-LIKE TYPOGRAPHY & SPACING
- Large hero type, short sentences.
- More whitespace than you think (section padding 72–120 desktop, 48–72 mobile).
- Use captions and figure labels instead of paragraphs (“Fig. 01”, “Protocol”, “Tested”).

B) “WOW INTERACTIONS” TO ADD (BY SECTION)
SECTION 1 HERO BANNERS (must feel unique)
- Use 3-slide hero carousel with:
  - Slide 1: cinematic product video loop + headline overlay
  - Slide 2: animated 3-layer skin depth visual
  - Slide 3: device mode showcase (icons + micro readout)
- Add subtle parallax (2–4px) between background and foreground text.
- Add “shoppable hotspots” on hero: tap reticle → floating product card → PDP.

SECTION 2 PROOF METRICS
- Count-up animation on scroll
- Add micro “data provenance” caption under the stat block:
  - “Based on internal usage + customer reporting” (placeholder)
- Add tiny sparkline micro-graphs behind each stat (very subtle, hairline).

SECTION 3 THREE NO-BS RULES (make it a showpiece)
- Make it a left/right interactive scrollytelling block:
  - Right: dynamic visualization changes as user swipes/scrolls between rules
  - Left: rule title + 2-line explanation + “Learn more”
- For “Ingredient delivery”: implement a 3-strip skin model where dots animate deeper when delivery is toggled on.
- For “Ingredients that work”: interactive ingredient chips reveal “micro cards” with role + mechanism.
- For “Barrier first”: an interactive “barrier shield” where layers stack and highlight as user taps.

SECTION 4 VIDEO WALL
- Thumbnails auto-play 1.5s preview on hover (desktop) / on viewport entry (mobile)
- Open full-screen modal with:
  - title, tags, product links
- Maintain IG ratio strictly.

SECTION 5 VISUAL PRODUCT CATALOG (wow moment)
- Make it a vertical “editorial spread” scroller:
  - Each plate has 2–3 products with precise hotspots
  - Hotspots breathe subtly (opacity pulse) not neon
  - Tooltip is a glass card with product image + name + price + “View”
- Add “lens focus” effect on tap: background slightly blurs while tooltip is open.

SECTION 6 SCIENCE TOOLS (tech explanation)
- Make it a horizontal “lab cards” carousel with:
  - micro diagrams (UV/LED, scanning, delivery)
  - mode icons
  - a short spec line (tabular numerals)
- On tap, expand card into a full panel (“sheet”) with one diagram and 3 bullets.

SECTION 7 ALL PRODUCTS TABS
- Tab switch uses crossfade + subtle reflow
- Product cards have:
  - fixed height
  - pinned price baseline
  - quick add button
- Hover state: card lift + image zoom 1.02 (desktop)

SECTION 8 SHOP BY CONCERN (upgrade to “Concern Studio”)
- Each concern tab shows:
  - one hero image (texture or in-use)
  - a “target layer” indicator (surface/mid/deep)
  - 2–3 product recs
- Include a small “protocol hint”: “AM/PM” chip and session time chip (where relevant).

SECTION 9 AM/PM ROUTINE
- Make it a timeline with step cards that animate in sequence as user scrolls.
- Add “Add routine” CTA (bundle) with a mini preview drawer.

SECTION 10 REVIEWS
- Use a premium carousel with:
  - pull-quote style
  - star summary
  - filters
- Add “customer videos” inside reviews as small tiles.

SECTION 11 CELEBRITY USERS
- Use a clean gallery with consistent grading
- Add subtle hover reveal with caption.

SECTION 12 FOUNDER STORY
- Add an editorial layout:
  - portrait + signature block
  - pull quote highlight
  - short paragraph blocks with generous spacing

SECTION 13 FINAL CTA
- Add a subtle blue bloom background and a product silhouette watermark (5% opacity)
- CTA button primary.

C) PDP WOW FEATURES (must feel like Apple product page)
- Add “scroll chapters” with a mini progress indicator (01/06)
- Add an interactive “Application Studio”:
  - realistic face image
  - click zones show arrows + pressure + time
  - Start session timer with progress ring
- Add “Formula Blueprint” diagram:
  - 3-layer stack with labels and micro annotations
- Before/after slider (PDP only) styled like an instrument comparison tool.

D) PERFORMANCE & POLISH CHECKLIST (must enforce)
- Every section has at least one high-quality visual element.
- No wall-of-text blocks. Convert text into:
  - bullets
  - captions
  - figure labels
  - diagrams
- Fix all layout issues:
  - product cards fixed height
  - price pinned baseline
  - consistent spacing tokens only
- Visual hierarchy is crisp:
  - 1 hero idea per section
  - supporting details hidden in accordions or sheets.

E) DELIVERABLE
- Apply the WOW layer across Home, PLP, PDP templates.
- Show prototype interactions for: hero hotspots, video modal, depth toggle, tabs, application studio timer.