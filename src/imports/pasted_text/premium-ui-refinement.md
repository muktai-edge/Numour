APPLY TO EXISTING FILE (bobbin-pug-24461543.figma.site): 
Upgrade experience by 200% through (1) pixel-perfect component discipline, (2) Apple-style pacing, and (3) subtle premium interactions. 
Do NOT change the IA/sections; refine and elevate.

1) PIXEL-PERFECT SYSTEM CLEANUP (HIGHEST PRIORITY)
A) Create a “Layout & Spacing Tokens” set:
- Spacing: 8, 12, 16, 24, 32, 40, 56, 72
- Radius: 12 (buttons), 16 (cards), 20 (hero plates), 999 (chips)
- Button heights: 48 mobile / 44 desktop
- Chip height: 28–30 consistent
- Hairline border: single style everywhere (same color, same 1px)

B) Typography normalization:
- Define only 6 text styles used everywhere:
  - H1, H2, H3, Body, Caption, Overline
- Lock line-height and weights; remove duplicates.
- Use tabular numerals for all stats/prices.

C) Product cards: rebuild as one component with strict constraints
- Fixed height across grid rows.
- Media fixed 1:1.
- Tags max 2 + “+N”.
- Title clamp 2 lines.
- Price row pinned baseline.
- CTA pinned bottom fixed height.
Replace every product card instance with this component and ensure grid alignment.

D) Badge/discount standardization:
- Discount uses one chip style, always in price row, never floating.
- Only 1 badge max (Bestseller/New), consistent position.

2) APPLE-STYLE PACING (SECTION RHYTHM)
A) Add “Chapter Progress Indicator” (homepage + PDP)
- Minimal vertical indicator showing:
  - current section number (01/09)
  - section label
- Updates on scroll.
- Tap opens a “Table of Contents” sheet with jump links to sections.

B) Add consistent “Figure” language
- Every science module must have:
  - “Fig. 0X — …” caption
  - 1-line explanation
- Remove long paragraphs; convert to captions + bullets in a sheet.

3) WOW INTERACTIONS (SUBTLE, PREMIUM, NOT GIMMICKY)
A) Section reveal animation:
- Each major section: hairline divider line draws in (400ms) + content fades up (8px).
No bounce.

B) Proof metrics:
- Count-up numbers on scroll.
- Add ultra-subtle sparkline behind each metric tile (hairline, low opacity).

C) Delivery Engine (3-layer strips):
- Dot movement and line draw on toggle.
- Add “Format tabs” (Gel / Jelly / Device) that changes dot path slightly and updates a small “Session time” chip.

D) Ingredient Explorer:
- Ingredient chips open a bottom sheet:
  - Role, “where it fits” (AM/PM), pairing chips
  - Link: “View full INCI” (anchor on PDP)

E) Visual catalog hotspots:
- Hotspots open a micro product tooltip.
- While tooltip open: background plate gets a slight blur (lens focus effect).
- Tooltip card layout standardized: image, name, price, View CTA.

F) Video wall:
- Thumbnails preview 1.5s on hover (desktop) / on viewport entry (mobile).
- Tap opens modal with title + product links.

G) PDP Application Studio:
- Face map zone taps show arrows + time + pressure.
- Start session timer (thin ring) with Start/Pause.
- Add in-page sticky tabs on PDP: Overview / Modes / How to use / Reviews / FAQ.

4) PAGE-BY-PAGE QUALITY PASS (NO NEW SECTIONS, JUST UPGRADE)
Homepage:
- Ensure Philosophy section feels like an interactive hub (3 rules with changing visualization).
- Ensure Tools section uses consistent glass cards + interactive capability selection.
- Ensure product tabs are unambiguous and transitions are smooth.

PLP:
- Filters and sorting polished; sticky filter bar on mobile.
- Product grid perfectly aligned.

PDP:
- Above fold: clean, uncluttered, conversion-first.
- What/Why/Wow interactive strip improved (swipeable, expands).
- Metrics, reviews, FAQ clean.

5) QA PAGE (MANDATORY)
Create a “Pixel Perfect QA” page:
- Show a product card grid proving equal heights and pinned baselines.
- Show spacing token examples.
- Show typography styles.
- Show 3 state examples: default/hover/active for cards, chips, buttons.

DELIVERABLE:
Update the existing file with these refinements and interactions. The final should feel 200% more premium, engineered, and polished — Apple/Dyson/Leica-level craft.